import SwiftUI

extension Color {
    // 기본 배경색
    static let backgroundPrimary = Color.white
    // 보조 버튼 색상  
    static let buttonSecondary = Color.blue
    // 기타 필요한 색상들...
    static let secondary03 = Color.gray.opacity(0.3)
} 